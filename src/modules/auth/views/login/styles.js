import { StyleSheet } from "react-native";
import { GLOBAL_BIG_SPACE } from "../../../../utils/GlobalStyles";

const LoginStyles = StyleSheet.create({
  containerStyles: {
    flex: 1,
    padding: GLOBAL_BIG_SPACE,
  },
  contentStyles: {
    flex: 1,
  },
});

export default LoginStyles;
